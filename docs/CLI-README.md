# Incursa Code Generator CLI Tool (Status)

This document is retained for historical context.

## Current Repository Status

This repository currently ships a Roslyn source generator package (`src/Incursa.Generators`).

A maintained CLI entrypoint is not present in this repository snapshot.

Use package/source-generator integration documented in `docs/README.md`.

## Why This Matters

If you are generating JSON for this repo, do not rely on CLI-specific workflows from older docs. Use the JSON contracts and schema files under `docs/schemas`.
