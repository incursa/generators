{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://bravellian.com/schemas/guid-backed-type.schema.json",
  "title": "GUID Backed Type",
  "description": "Schema for Bravellian GUID-backed value types (.guid.json)",
  "type": "object",
  "required": ["name", "namespace"],
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the GUID type",
      "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
    },
    "namespace": {
      "type": "string",
      "description": "The .NET namespace for the generated type",
      "pattern": "^[A-Za-z_][A-Za-z0-9_.]*$"
    },
    "properties": {
      "type": "array",
      "description": "Additional properties to add to the GUID type",
      "items": {
        "type": "object",
        "required": ["name", "type"],
        "properties": {
          "name": {
            "type": "string",
            "description": "The property name",
            "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
          },
          "type": {
            "type": "string",
            "description": "The .NET type for this property",
            "examples": ["string", "int", "bool", "DateTime", "decimal"]
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false,
  "examples": [
    {
      "name": "CustomerId",
      "namespace": "Bravellian.Identifiers"
    },
    {
      "name": "ProductId",
      "namespace": "Bravellian.Identifiers",
      "properties": [
        {
          "name": "Category",
          "type": "string"
        },
        {
          "name": "IsActive",
          "type": "bool"
        }
      ]
    }
  ]
}
